defaults:
  - default

training:
  batch_size: 8
  learning_rate: 5.0e-5
  total_steps: 5000
  warmup_steps: 100
  output_dir: outputs/real_finetune

data:
  data_root: data/real_episodes
  sim_episodes: 0
  real_episodes: 100
  use_augmentation: false

checkpoints:
  fusion_checkpoint: outputs/sim_training/best_model.pt
